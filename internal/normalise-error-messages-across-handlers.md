# Normalise error messages across handlers

The test depended on wall-clock ordering. It now uses a fake clock and passes deterministically.

Change #3 of 6 on branch `pr/20260811-105826-3-normalise-error-messages-across-handlers`.
